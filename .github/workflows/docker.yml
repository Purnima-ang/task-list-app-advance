name: Build and Deploy

on:
  push:
    branches: [ master,task_advance ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        pip install -r web/requirements.txt
        pip install pytest  # Add this line to install pytest

    - name: Set PYTHONPATH for pytest
      run: |
        echo "PYTHONPATH=$(pwd)/web" >> $GITHUB_ENV  # Set PYTHONPATH environment variable

    - name: Run tests with pytest
      run: |
        pytest web/tests  # Adjust path if needed

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push web image
      uses: docker/build-push-action@v5
      with:
        context: ./web
        push: true
        tags: purnimang/task_app:latest
